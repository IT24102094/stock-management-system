spring.application.name=stock-management-system

# Database Configuration
spring.datasource.url=jdbc:mysql://localhost:3306/stock_management_db?useSSL=false&allowPublicKeyRetrieval=true&serverTimezone=UTC
spring.datasource.username=root
spring.datasource.password=Jathu@123
spring.datasource.driver-class-name=com.mysql.cj.jdbc.Driver

# JPA/Hibernate Configuration
# Use 'update' to automatically create/update tables without dropping existing data
# Use 'create' to drop and recreate tables (will lose data)
# Use 'validate' to only validate schema matches entities
spring.jpa.hibernate.ddl-auto=update
spring.jpa.show-sql=true
spring.jpa.properties.hibernate.dialect=org.hibernate.dialect.MySQLDialect
spring.jpa.properties.hibernate.format_sql=true

# Server Configuration
server.port=8080
server.servlet.session.timeout=30m

# Logging Configuration
logging.level.com.stockmanagement=DEBUG
logging.level.org.springframework.security=DEBUG
logging.level.org.springframework.web=DEBUG

# Security Configuration
spring.security.user.name=admin
spring.security.user.password=admin123

# Database Initialization - Completely Disabled
# Changed from 'always' to 'never' - tables will be created by Hibernate only
# NO sample data will be loaded
spring.sql.init.mode=never
# Commenting out data locations to prevent any SQL initialization
# spring.sql.init.schema-locations=classpath:db/schema/*.sql
# spring.sql.init.data-locations=classpath:db/data/initial-data.sql
spring.sql.init.continue-on-error=true
spring.sql.init.separator=;

# Email Configuration (Configure these in production)
spring.mail.host=smtp.gmail.com
spring.mail.port=587
spring.mail.username=it24102094@my.sliit.lk
spring.mail.password=kmnp wzlp kygy mdvk
spring.mail.properties.mail.smtp.auth=true
spring.mail.properties.mail.smtp.starttls.enable=true

# Email is disabled by default in development mode
# If you want to enable it, configure the settings above and set this to true
spring.mail.enabled=true

# File Upload Configuration
spring.servlet.multipart.max-file-size=5MB
spring.servlet.multipart.max-request-size=10MB
